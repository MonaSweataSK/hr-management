{
  "name": "hr-management",
  "version": "1.0.0",
  "description": "HR Management app with React + TypeScript frontend and Node.js backend",
  "private": true,
  "scripts": {
    "backend": "node backend/dist/server.js",
    "backend:watch": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "start": "node backend/dist/server.js",
    "dev": "concurrently \"npm run backend:watch\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
